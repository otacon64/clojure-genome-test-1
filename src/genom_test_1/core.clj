(ns genom-test-1.core)

(defn foo
  "I don't do a whole lot."
  [x]
  (println x "Hello, World!"))

(defn try []
  (println "try harder"))


(def it-works "WOW IT WORKS")

it-works

(def omg-new "LOLKEK")
